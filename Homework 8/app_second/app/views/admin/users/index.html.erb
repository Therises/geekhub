<table class="table">
	<tr>
		<th>ID</th>
		<th>Email</th>
		<th>Admin</th>
		<th>Action</th>
	</tr>
	<% @users.each do |user| %>
		<tr id=<%= "user_#{user.id}" %>>

			<%= render user %>
		</tr>
	<% end %>
</table>

<%= will_paginate @users %>