<%= link_to "Back", city_path(@plane.city_id) %>
<% if current_user && current_user.admin %>
	<h1>Editing plane <%= @plane.name %></h1>
	<%= render 'form',  path: plane_path(@plane), method: :patch, btn_name: 'Update plane' %>
<% else %>
	<h1>Unauthorized Acces</h1>
<% end %>